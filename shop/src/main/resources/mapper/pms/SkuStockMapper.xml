<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learn.shop.dao.pms.SkuStockDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.learn.shop.entity.pms.SkuStockEntity">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="sku_code" property="skuCode"/>
        <result column="price" property="price"/>
        <result column="stock" property="stock"/>
        <result column="low_stock" property="lowStock"/>
        <result column="pic" property="pic"/>
        <result column="sale" property="sale"/>
        <result column="promotion_price" property="promotionPrice"/>
        <result column="lock_stock" property="lockStock"/>
        <result column="sp_data" property="spData"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, product_id, sku_code, price, stock, low_stock, pic, sale, promotion_price, lock_stock, sp_data
    </sql>

    <select id="getSkuStockByProduct" resultType="com.learn.shop.vo.pms.SkuStockListVo">
        select s.id, s.sku_code as skuCode, s.sp_data as spData, s.price, s.stock, s.low_stock as lowStock
        from pms_sku_stock s
        where s.product_id = #{productId}
    </select>

    <select id="getSkuStockBySkuCode" resultType="com.learn.shop.vo.pms.SkuStockListVo">
        select s.id, s.sku_code as skuCode, s.sp_data as spData, s.price, s.stock, s.low_stock as lowStock
        from pms_sku_stock s
        where s.sku_code = #{skuCode,jdbcType=VARCHAR}
    </select>

</mapper>
